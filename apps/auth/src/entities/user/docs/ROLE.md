# ✅ Почему Роль — это часть домена User, а не самостоятельный домен

Потому что, приложение тестовое, в котором:

- Нет фич, где Role используется сам по себе
- Нет API вроде /roles/create, /roles/assign
- Нет UI для управления ролями
