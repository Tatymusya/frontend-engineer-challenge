/**
 * Глобальные константы приложения.
 * Содержит настройки, связанные с метаданными, таймаутами, пагинацией, загрузкой файлов,
 * функциональными возможностями и политиками повторных попыток.
 */
export const APP_CONSTANTS = {
  /**
   * Метаданные приложения
   */
  APP_NAME: 'Auth App',
  APP_VERSION: '1.0.0',
  
  /**
   * Тайм-ауты (в миллисекундах)
   */
  DEFAULT_TIMEOUT: 10000, // 10 секунд — стандартный таймаут операций
  SHORT_TIMEOUT: 5000,    // 5 секунд — короткие запросы
  LONG_TIMEOUT: 30000,    // 30 секунд — длительные операции
  DEBOUNCE_DELAY: 300,    // 300 мс — задержка для debounce (например, при поиске)
  
  /**
   * Настройки пагинации
   */
  DEFAULT_PAGE_SIZE: 20,  // Количество элементов на странице по умолчанию
  MAX_PAGE_SIZE: 100,     // Максимально допустимое количество элементов на странице
  
  /**
   * Флаги функциональности (управление возможностями приложения)
   */
  ENABLE_MFA: true,                // Включена двухфакторная аутентификация
  ENABLE_SOCIAL_LOGIN: true,       // Разрешён вход через социальные сети
  ENABLE_PASSWORD_RESET: true,     // Доступна функция восстановления пароля
  
  /**
   * Политики повторных попыток
   */
  DEFAULT_RETRY_ATTEMPTS: 3,       // Количество попыток повтора при сбое
  RETRY_DELAY_MS: 1000,            // Задержка между попытками (1 секунда)
} as const;